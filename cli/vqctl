#!/usr/bin/env bash
# vqctl — launcher for the Explore Vieques control panel.
# Usage: ./cli/vqctl   (or add cli/ to your PATH)
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec node "$DIR/vqctl.js" "$@"
